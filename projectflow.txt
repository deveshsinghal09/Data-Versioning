1. Create git repo and clone it in local.
2. Create mycode.py and add code to it (it will add a csv file to a new "data" folder).
3. Do a gir add-commit-push before initializing dvc.
# pip install dvc
4. Now we don "dvc init" (Creates .dvcignore, .dvc)